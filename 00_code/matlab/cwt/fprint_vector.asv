function fprint_vector(vect, lenx, leny, filename)

fp = fopen(filename, 'w');
if(fp == -1)
    error("Couldn't open file: %s", filename);

for i=1:lenx
    fprintf(fp, "%s", regexprep(num2str(imag(cfs(i,:))),'\s+','\n'));
    fprintf(fp, "\n");
end


end

